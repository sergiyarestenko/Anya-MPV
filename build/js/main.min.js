"use strict";

var observeHeader = function observeHeader(e, s) {
    new IntersectionObserver(function (s) {
      s.forEach(function (s) {
        s.isIntersecting ? e.classList.remove("scrolled") : e.classList.add("scrolled");
      });
    }).observe(s);
  },
  changeHeaderClassOnScroll = function changeHeaderClassOnScroll() {
    var e = document.getElementById("header"),
      s = document.getElementById("observer");
    observeHeader(e, s);
  };
changeHeaderClassOnScroll();